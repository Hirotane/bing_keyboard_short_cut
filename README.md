## How to uglify the project

`npm install uglify-js -g`: install uglify-js package

`chmod +x uglify.sh`: give excute permission to the shell script

`./uglify.sh`: uglify all js files in the project
